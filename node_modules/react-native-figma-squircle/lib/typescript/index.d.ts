import { ViewProps } from 'react-native';
import { PropsWithChildren } from 'react';
import { Color } from 'react-native-svg';
interface SquircleParams {
    cornerRadius?: number;
    topLeftCornerRadius?: number;
    topRightCornerRadius?: number;
    bottomRightCornerRadius?: number;
    bottomLeftCornerRadius?: number;
    cornerSmoothing: number;
    fillColor?: Color;
    strokeColor?: Color;
    strokeWidth?: number;
}
interface SquircleViewProps extends ViewProps {
    squircleParams: SquircleParams;
}
declare function SquircleView({ squircleParams, children, ...rest }: PropsWithChildren<SquircleViewProps>): JSX.Element;
export { SquircleView };
export type { SquircleParams };
